# Docs

Reference material that does not belong directly in source folders.

- `ci-example.yml` — template GitHub Actions workflow. Copy to `.github/workflows/ci.yml` to enable CI for backend, frontend, and contracts.
